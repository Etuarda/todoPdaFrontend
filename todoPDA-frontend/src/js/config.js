export const config = Object.freeze({
  baseUrl: "https://todopda.onrender.com",
  prefix: "/api",
  storageKeys: Object.freeze({
    session: "todopda.session",
    vision: "todopda.vision"
  })
});
